import { Link, NavLink, useNavigate } from "react-router-dom"

export const Footer = () => {

    return (
        <footer style={{ textAlign: "center"}}>
            {/* <NavLink to="/">
            HOME
            </NavLink> */}
        </footer>
    );

}